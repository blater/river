# tic-6f81: chunked relational WAL contract

Date: 2026-09-15. Source: `dec2dafc615710f87ff00b2c45ae00a7c2bf861f`.
Decision: the current source and existing tests satisfy the investigation.
Independent integrator review accepted the source trace, compositional proof,
and conclusion that no additional blocker is required.

This was a read-only source and test audit. No build, test, workload, or
diagnostic command was run for this record. Test names below identify inspected
existing evidence; they are not a claim of a fresh test execution.

Path abbreviations in the matrix expand exactly as follows:

- engine `main/.../X` = `river-engine/src/main/java/io/riverdb/engine/table/X`;
- engine `test/.../X` = `river-engine/src/test/java/io/riverdb/engine/table/X`;
- WAL `main/.../X` = `river-wal/src/main/java/io/riverdb/wal/local/X`;
- WAL `test/.../X` = `river-wal/src/test/java/io/riverdb/wal/local/X`.

| Contract clause | Current owner and existing evidence | Disposition |
| --- | --- | --- |
| Plan and chunk boundary | Engine `main/java/io/riverdb/engine/table/IndexedHybridLogicalSizing.java:20-51,212-224` measures the sealed logical item stream, chunk count, and encoded WAL bytes. `IndexedRelationalWalSizing.java:14-43,66-75` and `IndexedRelationalWalPlan.java:69-97` pack whole items up to `WalRecordCodec.MAX_PAYLOAD_BYTES - IndexedRelationalWalCodec.HEADER_BYTES`. This is the persisted-record structural bound; the transaction budget admits the whole encoded decision and does not redefine record framing. `IndexedRelationalWalCodecTest.worstMutationCountChunksWithinPhysicalRecordLimit` proves a 384-mutation decision spans bounded records (`test/.../IndexedRelationalWalCodecTest.java:239-273`). `IndexedRelationalWalRecoveryTest.streamsMoreThanOneProviderBatchAndReplaysOnlyTheFinalDecision` crosses the former fixed slot boundary with more than 16 chunks (`test/.../IndexedRelationalWalRecoveryTest.java:110-159`). | Satisfied. No guessed transaction, record, or cohort cap. Integer exhaustion is the named addressability boundary. |
| Reservation and ownership | `IndexedPreparedLogicalCommit.prepare` acquires the transaction WAL-byte lease and retained plan-array storage before enqueue, then reserves exact plan capacity (`main/.../IndexedPreparedLogicalCommit.java:47-78`). `IndexedTransactionResourceAdmission.java:43-76,90-105` owns those leases. `IndexedHybridGroupPreflight.java:51-113` compiles each accepted member, builds its pre-reserved plan without growth, and rejects an actual chunk or encoded-byte demand beyond admission before append. `IndexedTransactionResourceAdmissionTest.repeatedAbsoluteWorkspaceAdmissionCannotDoubleChargeAfterAllocationFailure` covers retained lease growth, idempotence, and release (`test/.../IndexedTransactionResourceAdmissionTest.java:11-34`). | Satisfied. One session-owned plan and one admitted whole-decision budget. |
| Provider-owned encode storage and copies | WAL `main/java/io/riverdb/wal/local/LocalWal.java:72-86,934-946` owns one reusable direct append record and payload view. `LocalWalRecordBatchAppender.java:85-123` and `LocalWalDecisionBatchAppender.java:24-61` ask the canonical batch to encode each ordinal directly into that storage and validate the exact byte count. Engine `IndexedRelationalWalCodec.java:26-46,270-289` writes headers/items directly and performs the one necessary mutation-payload lifetime copy. `IndexedRelationalWalPlan.java:123-131` reconciles copied payload bytes. `LocalWalTest.reservationUsesProviderStorageWithoutPayloadCopy` (`test/.../LocalWalTest.java:338-355`), `LocalWalAllocationTest.warmedBatchedAppendForceAndBorrowedReadAllocateNothing` (`test/.../LocalWalAllocationTest.java:84-128`), and the relational copy assertions in `IndexedTableAllocationTest.java:120-270` cover provider reuse and copy accounting. | Satisfied. Capacity growth occurs before the steady append loop; source inspection finds no per-chunk allocation. |
| Direct and group calls | The prepared singleton/direct path is `IndexedHybridCommitGroup.commitDirect` with one member (`main/.../IndexedHybridCommitGroup.java:59-103`). Shared cohorts reach the same `preflight`, `append`, `force`, and publication methods through `IndexedGroupCommitBatch.java:61-95,117-142,237-260`. Both use `IndexedRelationalWalGroupAppender`, which cumulatively maps each member plan's record count to one `LocalWalDecisionBatch` (`main/.../IndexedRelationalWalGroupAppender.java:41-78,108-177`). Its only multi-chunk-dependent adapter state is monotone `groupEnds`; the binary search returns the first member end greater than the record ordinal. WAL `LocalWalTest.aggregateAdmissionAppendsIndependentTwoOneThreeDecisionsWithOneForce` exercises transactions with 2, 1, and 3 records and verifies decision/CSN placement plus one force (`test/.../LocalWalTest.java:476-514`). Engine `IndexedRelationalWalCommitTest.java:338-475` proves ordinary prepared members share one force, retain independent consecutive decisions, publish, and survive two reopens. | Satisfied compositionally. The adapter has no chunk-payload-dependent branch requiring a Cartesian direct/group large fixture. |
| Bytes, records, append, force, and publication | Engine plan fields retain whole and per-chunk totals/digests (`IndexedRelationalWalPlan.java:51-66,114-152`); durable headers repeat them (`IndexedRelationalWalCodec.java:167-195`). WAL `LocalWalBatchAdmission.java:10-33` sums every encoded record before writing. `LocalWal.java:1138-1174` accepts the exact byte end and record count. `LocalWalForceTarget.java:40-67` requires exact append range/count identity. `LocalWalDecisionBatchAppender.java:41-59` places one decision/CSN on each transaction's final record. `LocalWalForceCoordinator.java:10-37` captures and forces the aggregate once. `IndexedHybridGroupPublication.java:19-46` installs the accepted members and advances one final publication frontier. Exact range/count evidence is `LocalWalForceTargetTest.capturesBeforeIoAndRetainsExclusiveCoverageUntilRelease` (`test/.../LocalWalForceTargetTest.java:32-76`); multi-record decision evidence is the `2/1/3` test above; relational final-chunk-only visibility is `IndexedRelationalWalRecoveryTest.productionDispatchRetainsContiguousGroupAndPublishesOnlyFinalChunk` (`test/.../IndexedRelationalWalRecoveryTest.java:41-79`). | Satisfied. Physical records reconcile to one logical member decision, one aggregate force target, and one publication outcome. |
| Partial append | Both WAL appenders mark `STORAGE_MAY_HAVE_CHANGED` before each write and fence after a later encode or write failure (`LocalWalRecordBatchAppender.java:91-129`; `LocalWalDecisionBatchAppender.java:29-97`). Hybrid append and cancellation convert possible storage change into a writer fence (`engine main/.../IndexedHybridCommitGroup.java:180-206,261-287`). `LocalWalTest.decisionBatchReportsPhysicalProgressWhenLaterEncodingFails` proves later failure after accepted bytes reports possible storage change and fences (`test/.../LocalWalTest.java:516-538`). `IndexedGroupCommitFaultTest.groupedFacadeCommitFailureWithholdsAcknowledgmentAndFencesAdmission` includes partial file write and terminal cleanup (`test/.../IndexedGroupCommitFaultTest.java:706-795`). | Satisfied. Cleanup depends on physical progress, not payload kind or which transaction boundary the prior record carried. |
| Force failure and indeterminate outcome | `LocalWalForceCoordinator.java:10-37` distinguishes captured, locally forced, and durability-complete states and fences every incomplete or exceptional force. Engine `IndexedHybridCommitGroup.java:209-218,279-287` and `IndexedGroupCommitBatch.java:322-339` withhold acknowledgment and choose unforced versus uncertain terminalization from append progress. `LocalWalForceTargetTest.failedForceRetainsIdentityWithoutClaimingDurability`, `fencingDuringIoCannotCloseTheFileOrTurnLocalSuccessIntoAcknowledgement`, and `unexpectedProviderFailureUnwindsForceOwnershipAndAllowsFencedClose` cover these states (`test/.../LocalWalForceTargetTest.java:79-161`). Engine `IndexedGroupCommitFaultTest.forceFailureAfterPressureSplitTerminalizesAcceptedPrefixAndDeferredSuffix` proves cleanup and crash/reopen rollback (`test/.../IndexedGroupCommitFaultTest.java:562-657`); its forced publication failures recover exactly once (`:676-703`). | Satisfied. Force behavior consumes only the captured aggregate range/count and has no relational-chunk-content branch. |
| Cancellation | `LocalWal.java:372-393` permits logical-stream cancellation only before any bytes or retained target; partial streams fence. `IndexedHybridCommitGroup.java:261-287,312-327` resets staged work only when storage cannot have changed. Exact forced-target release clears pending state once (`LocalWal.java:512-529`). `IndexedPreparedGroupPublicationTest.cancellationBeforeForceKeepsFrontierInvisibleAndAllowsNextCommit` covers prepared-group cancellation and reuse (`engine test/.../IndexedPreparedGroupPublicationTest.java:106-155`); WAL ownership/cancel evidence is `LocalWalTest.java:645-674`; the partial-progress test above covers the post-write fence rule. | Satisfied. Cancellation branches on accepted physical progress, not chunk count. |
| Recovery | WAL `LocalWalRecovery.java:32-105` accepts complete checksummed force groups and truncates incomplete suffixes. Engine `IndexedWalRecovery.java:59-118` dispatches contiguous relational records and removes a complete-force but decisionless relational suffix. `IndexedRelationalWalRecovery.java:28-69` retains chunks and invokes replay only after the final decision; `IndexedRelationalWalValidation.java:31-62` validates totals and final-chunk decision placement. Large-decision evidence covers final-only visibility (`IndexedRelationalWalRecoveryTest.java:41-79`), more than 16 chunks and exactly one replay (`:110-159`), decisionless suffix repair plus a second reopen (`:161-227`), and replicated suffix repair (`:229-313`). | Satisfied. |

## Limits and handoff

This evidence establishes correctness, ownership, and existing allocation/copy
behavior. It makes no throughput claim and ran no new test or workload. Ordinary
TPC-C decisions normally fit one record, so duplicating every fault across large
records and direct/group cardinalities would add no newly identified invariant.

The audit found no absent clause and no separate code or evidence ticket is
required. `tic-92e3` may proceed with its selected force-overlap mechanism while
preserving the plan, provider buffer, decision boundaries, force-target identity,
and terminal cleanup mapped here.
