---
id: tic-0788
status: closed
type: story
priority: 2
delivery: code
created: 2026-09-11
parent: tic-c8d1
---
# Simplify TpccMetrics

File: `river-bench/src/main/java/io/riverdb/bench/tpcc/TpccMetrics.java`. Baseline slopwatch score: **135.044**.

## Approach

Give latency histograms, protocol traffic, and retry/attempt accounting concrete
owners. Keep transaction outcomes, allocation observations and cutoff state in
TpccMetrics. Migrate terminal/report/artifact/gate consumers directly and remove
the superseded accessors. Share saturating primitive-counter sum/merge operations
and cache enum counts or private iteration arrays once. Preserve measured/drain
semantics, correlation diagnostics, protocol denominators, self-merge, and the
overall overflow result, including overflow discovered by queries.

## Acceptance

The file and any extracted files score below 90 with the unchanged full-repository
`slopwatch -follow=false -include-tests -format json -limit 0 .` scan. Preserve
behavior, public contracts and failure cleanup. No suppressions, scorer changes,
new per-row allocation, or arbitrary file splitting. Luna/high codes; Sol/high
reviews; the lead reviews architectural effects across adjacent owners.
Run focused `river-bench` checks and the epic's light performance check, record the
before/after score and result, then integrate this ticket independently.


## Validation

Implementation `6c56d705` on `ticket/tic-0788-tpcc-metrics`.
TpccMetrics falls from 135.044 to 73.925; retry metrics score 78.761,
latency and protocol metrics 0. Existing callers use the owners directly.
Root and Sol reviewed counter saturation, query-discovered overflow, self-merge,
measured/drain accounting, diagnostics and allocation. Three owners are created
with each metrics instance; no per-record or query allocation was added.

Eight focused metrics, promotion and artifact tests passed, with bench checks
and TPS installation (`--no-daemon`). Log: `/private/tmp/river-tic-0788-gradle.log`.

The Java TPS harness exercises these metrics directly. Identical tiny/standard,
four-terminal, one-warehouse runs used seed 42, maximum attempts 32, five seconds
warmup and ten seconds measured. Installed first-46 control
`score-first46-0788-control`: **393.800 TPS**; candidate
`tic-0788-6c56d705-jvm`: **468.500 TPS**. Both reported zero retries/errors,
valid recovery/checkpoint and deadlock accounting, valid capture, no cutoff
transactions, and zero remaining transactions, locks or waits. One short pair
supports no speedup claim and shows no regression. Logs and artifacts:
`/private/tmp/river-score-20260911/tic-0788-control` and `tic-0788-candidate`.
Command: `tools/tps-test.sh --profile=tiny --mix=standard --terminals=4
--warehouses=1 --seed=42 --maximum-attempts=32 --warmup-seconds=5
--measured-seconds=10`, with the recorded version and output directory.

Delivered in `perf-checkpoint-20260911-score-first51`.
