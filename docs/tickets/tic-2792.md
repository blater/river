---
id: tic-2792
status: closed
type: story
priority: 2
delivery: code
created: 2026-09-11
parent: tic-c8d1
---
# Simplify TransactionProgramSteps

File: `river-engine/src/main/java/io/riverdb/engine/TransactionProgramSteps.java`. Baseline slopwatch score: **92.804**.

## Approach

Unify singleton result capture and empty-result branching shared by point and scan execution inside the existing step owner. Keep scan cardinality checking and close status precedence separate and unchanged. Reuse the existing reader and result carriers; add no per-step state or allocation.

## Acceptance

The file and any extracted files score below 90 with the unchanged full-repository
`slopwatch -follow=false -include-tests -format json -limit 0 .` scan. Preserve
behavior, public contracts and failure cleanup. No suppressions, scorer changes,
new per-row allocation, or arbitrary file splitting. Luna/high codes; Sol/high
reviews; the lead reviews architectural effects across adjacent owners.
Run focused `river-engine` checks and the epic's light performance check, record the
before/after score and result, then integrate this ticket independently.

## Validation

Accepted `4fb5f823`: point/scan singleton execution shares the identical capture
sequence inside the current owner. Reader positioning, dataflow/output ordering,
cardinality, empty-target branching and scan-close precedence remain unchanged;
no added state/allocation. Score 92.804 → 59.184. Sol/high and lead approved.
All 10 transaction-program tests and engine checks/installTps passed in 12s.
Log: `/private/tmp/river-tic-2792-build.log`.

Used Java `tools/tps-test.sh` because it exercises transaction programs.
GraalVM25.0.4, tiny/standard, four terminals, one warehouse, seed42, max32attempts,
5s warmup, SERIALIZABLE, no-wait-stress, unchanged default resource budgets.
Frozen first25 control versus candidate, 10s: 470.6 / 464.9 TPS, retries0/1.
The new fully reconciled deadlock retry triggered a 30s pair: 502.467 / 558.367
TPS, retries0/1; final adjacent unchanged control: 551.167 TPS, two reconciled
payment deadlock retries. This reproduces the retry behavior and higher rate in
unchanged code. Every run returned OK, zero errors, valid capture/reconciliation,
checkpoint completion and exit0. No observed regression or speedup claim.
Artifacts under `/private/tmp/river-score-20260911/`:
`tic-2792-control`, `tic-2792-candidate`, `tic-2792-control-long`,
`tic-2792-candidate-long`, `tic-2792-control-adjacent`.
Labels: `score-first25-program-control`, `tic-2792-4fb5f823-program-capture`,
then suffix `-long`; final control label `score-first25-program-control-adjacent`.
These Java TPS figures are separate from the external harness results.
