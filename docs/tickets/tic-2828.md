---
id: tic-2828
status: in_progress
type: bug
assignee: blater
delivery: code
base-commit: a6d2393bcf5bf04b49893699429b289bf919062e
branch: ticket/tic-2828-page-generation-reuse
tags:
    - performance
    - storage
    - mvcc
    - p0
created: 2026-09-04T21:32:28.16136Z
---
# Reuse retired page generations before growing frame residency

Current-frame selection returns the first null slot before checking later historical generations that are already below the oldest visible sequence. Repeated commits to the same page therefore allocate one frame and direct buffer per commit until the entire configured cache is populated, violating the warmed allocation contract and wasting admitted memory.

## Design

Preserve MVCC and eviction semantics while ordering candidates by safety and cost: eligible retired historical generation, existing empty/null slot, then ordinary evictable current page. Keep one owning selector and existing probe cursor; add deterministic small-geometry tests.

## Acceptance Criteria

Repeated same-page publication reuses an unpinned generation whose valid-until sequence is no newer than the oldest visible sequence even when unused slots remain; a pinned or still-visible generation is never reused; distinct current pages still grow into unused admitted slots; the focused warmed commit allocation test passes without raising its threshold; affected engine tests pass with no new arbitrary bound.

## Notes

### 2026-09-05T16:59:30Z

The combined P0 baseline gate reached 868 tests before
`IndexedTableAllocationTest.warmedImmutableHeapIndexCommitReusesProductionState`
reported 72,704 allocated bytes against the existing 512-byte ceiling; its XML
SHA-256 is
`3f11f8c18fab6ce1cbc83b6eb9f39014d471c054f449f387c16665324324b71e`.
The exact test independently reproduces on clean pre-ticket commit
`9f756561f79d1ad0952c0ff4d38c07f670badd31` with 80,896 allocated bytes and
XML SHA-256
`9ba38e663c10c528f2c81b6e1e0b56d2229e787ef5596f322d6089da9a82fe1e`.
It also reproduces at `56ccd9d317e135a9f06cd2ec022bba2160c43a71`
with the same byte count and XML SHA-256
`deb001398f2c63056d10d88fa822438cff2d065dc6d2255efec881e3e5346392`.
This excludes the UNION, group-fencing, spill-boundary, and savepoint changes as
the cause; those tickets must not absorb the allocation fix.

