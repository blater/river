---
id: tic-3142
status: in_progress
type: story
priority: 2
delivery: code
created: 2026-09-11
parent: tic-c8d1
---
# Simplify LockTupleIntervalTableTest

File: `river-tx/src/test/java/io/riverdb/tx/LockTupleIntervalTableTest.java`. Baseline slopwatch score: **111.571**.

## Approach

Move per-test fixtures, tuple request builders, waiter lifetimes and allocation
cycle helpers into one package-private support owner. Keep all 18 scenario bodies
and their overlap, boundary, resource and allocation assertions unchanged.

## Acceptance

The file and any extracted files score below 90 with the unchanged full-repository
`slopwatch -follow=false -include-tests -format json -limit 0 .` scan. Preserve
behavior, public contracts and failure cleanup. No suppressions, scorer changes,
new per-row allocation, or arbitrary file splitting. Luna/high codes; Sol/high
reviews; the lead reviews architectural effects across adjacent owners.
Run focused `river-tx` checks and the epic's light performance check, record the
before/after score and result, then integrate this ticket independently.

## Validation

Implementation `71ca8e8e`; Luna/high, Sol/high and lead accepted. Test and
support both score 0. All 18 focused tests passed without failures, errors or
skips; transaction checks and installTps passed in 5s. Log:
`/private/tmp/river-score-jdbc-tic-3142-gradle.log`.
Light sample/all JVM workload: 4 workers, 1 warehouse, seed 42, max-retries 20,
5s warmup and 10s measured, version `tic-3142-71ca8e8e-jvm`: 313.35 TPS,
p99 60.260ms. Zero failed/unknown outcomes, valid invariants and graceful
inactive cleanup. Artifact: `river_harness_20260911_060238_92237ab1` under
harness runs. No observed regression; test-only change makes no speedup claim.
