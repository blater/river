---
id: tic-5049
status: closed
type: story
priority: 2
delivery: code
created: 2026-09-11
parent: tic-c8d1
---
# Simplify SqlBoundBlockPlans

File: `river-engine/src/main/java/io/riverdb/engine/sql/SqlBoundBlockPlans.java`. Baseline slopwatch score: **131.474**.

## Approach

Keep the necessary copied join plan snapshot: a live bound-context reference can
observe later reuse. Give its arrays, capture/reset and derived accessors one
concrete SqlBoundJoinSnapshot owner. Binding and plan description use it directly;
block plans retain schema, command, root access and liveness ownership. Preserve
block guards, defaults, statistics/estimate values, reset and explain order. Add
no per-stage allocation or duplicated policy.

## Acceptance

The file and any extracted files score below 90 with the unchanged full-repository
`slopwatch -follow=false -include-tests -format json -limit 0 .` scan. Preserve
behavior, public contracts and failure cleanup. No suppressions, scorer changes,
new per-row allocation, or arbitrary file splitting. Luna/high codes; Sol/high
reviews; the lead reviews architectural effects across adjacent owners.
Run focused `river-engine` checks and the epic's light performance check, record the
before/after score and result, then integrate this ticket independently.

## Validation

Source `5fb944cc`: SqlBoundBlockPlans **131.474 → 5.688**; the join snapshot
scores **67.668**, and touched plan consumers remain below 90. Root and Sol/high
reviewed capture/reset, copied-array lifetime, bounds/defaults and plan estimates.
The snapshot replaces the existing arrays' owner; it does not retain a mutable
join context or allocate per stage or rebind.

All 11 focused tests passed across SqlJoinProjectionEvaluatorTest,
SqlJoinPlannerTest, SqlJoinBlockPipelineTest, SqlNestedQueryTest, SqlExplainTest
and SqlJoinedSubqueryTest. Engine checks and source policy passed. Log:
`/private/tmp/river-tic-5049-gradle.log`.

The external sample/all workload passed at **308.10 TPS**, p99 **63.44 ms**,
500 retries, zero failed/unknown outcomes, successful invariants and graceful
shutdown to inactive. Configuration: four workers, one warehouse, seed 42,
20 retries, five seconds warmup, ten seconds measured, GraalVM 25; version
`tic-5049-5fb944cc-jvm`. Artifact:
`~/src/ingres/river-harness/runs/river_harness_20260911_083558_4a542b00`.
Accepted as a behavior-preserving refactor; no speedup claim.


## M5 integration promotion

Accepted in the eleven-ticket integration at source `2c5dd377`, checkpoint
`perf-checkpoint-20260911-score-first62-m5`. Current-platform clean checks
passed (1,952 tests, zero failures/errors, 18 existing skips), all touched files
score below 90, and independent integration review found no blocking issue.
The adjacent master/candidate/candidate/master JVM series passed at
562.82/526.70/501.22/422.15 TPS with valid invariants, zero failed/unknown
outcomes and graceful cleanup. No repeated candidate regression was observed.
Full evidence is in `docs/performance-checkpoints.md`. Native compilation
remains blocked on unchanged master by the separately tracked `tic-ae17`;
this acceptance certifies the JVM path, not native execution.
