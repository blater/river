---
id: tic-5306
status: in_progress
type: task
priority: 2
assignee: blater
delivery: code
base-commit: 5627543e624ac5a22d35e940e1f8bf6cf506b325
branch: ticket/tic-5306-test-sort
tags:
    - testing
    - maintenance
created: 2026-09-07T20:19:28.623931Z
---
# Reduce external-sort test cost without losing run-boundary coverage

Implement only the accepted sort findings from tic-37c1, preserving the named survivor coverage.


## Scope and validation

Use the accepted decisions in [tic-37c1](tic-37c1.md). Test-only changes; preserve
production behavior and named coverage. The lead owns serial Gradle validation
with --no-daemon, integration, and closure. No TPS or provenance redesign.

## Delivery evidence

The fixture now uses supported 4KB pages, an 8KB sort run and a 256KB cache.
Its row count is derived from the admitted run capacity: 65 complete runs plus
one row (64,481 rows here). Every ordered value and public key is still checked,
as are store and fixture cleanup. The separate 65,537-row ordinal test and
warmed allocation check remain unchanged.

Fresh unchanged method: 338.838s; candidate method: 79.428s. These are one
adjacent local pair, not a stable speed guarantee. The whole candidate class
passed all five tests using `./gradlew --no-daemon :river-engine:test --tests
io.riverdb.engine.sql.SqlBlockRowPagedStoreTest`. Baseline used the same method
with `--rerun-tasks` to ensure execution. Logs and XML:
`/private/tmp/river-test-streamline-evidence-20260907/sort-{baseline,candidate}.{log,xml}`.

Luna authored the fixture; the lead checked the admitted page/run minimums,
production run-capacity calculation, preserved boundaries and test results.
Production code is unchanged; TPS and production slopmark runs are not relevant
to this test-fixture change.
