---
id: tic-7339
status: closed
type: story
priority: 2
delivery: code
created: 2026-09-11
parent: tic-c8d1
---
# Simplify SqlBlockAggregateBinder

File: `river-engine/src/main/java/io/riverdb/engine/sql/SqlBlockAggregateBinder.java`. Baseline slopwatch score: **161.174**.

## Approach

Review `SqlBlockAggregateBinder.bindLoweredJoin`, `SqlBlockAggregateBinder.bind`, `SqlBlockAggregateBinder.publish` first. Separate their distinct validation, execution and cleanup responsibilities into concrete local operations; flatten status-dependent control flow while preserving ordering and ownership. Reuse an existing owner where one exists, and avoid new delegation layers that merely move branches.

## Acceptance

The file and any extracted files score below 90 with the unchanged full-repository
`slopwatch -follow=false -include-tests -format json -limit 0 .` scan. Preserve
behavior, public contracts and failure cleanup. No suppressions, scorer changes,
new per-row allocation, or arbitrary file splitting. Luna/high codes; Sol/high
reviews; the lead reviews architectural effects across adjacent owners.
Run focused `river-engine` checks and the epic's light performance check, record the
before/after score and result, then integrate this ticket independently.

## Validation

Accepted source `c51c9a8f`, Luna/high implemented and Sol/high/lead approved.
Invocation type validation/result selection/append now has one implementation;
metadata publication is a separate stateless operation. Lane mapping, status
order, hidden group-order columns and nullability are unchanged. No helper or
per-bind allocation added. Scores: binder 43.800 (161.174 before), invocation
7.427, metadata 0.

Ten focused scalar/grouped/join/view/nullability tests, engine checks and
benchmark installation passed with `--no-daemon`; log:
`/private/tmp/river-tic-7339-build.log`. The epic's light JVM sample/all passed
at 306.79 TPS, p99 60.260ms, zero failed/unknown outcomes, valid invariants and
graceful cleanup, consistent with the adjacent short control 308.37 TPS.
Artifact: `/Users/blater/src/ingres/river-harness/runs/river_harness_20260911_041025_456c3d9d`.

Delivered at `perf-checkpoint-20260911-score-first19`.
