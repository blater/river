---
id: tic-7352
status: closed
type: story
priority: 2
assignee: blater
parent: tic-e5ff
delivery: code
base-commit: ab4769c
branch: ticket/tic-7352-force-target-ownership
delivered-commit: 195c6419afda3e288caf1f57f6e29aae5e51101c
checkpoint-tag: perf-checkpoint-20260907-force-target-ownership
tags:
    - architecture
    - wal
    - transactions
deps:
    - tic-b368
links:
    - tic-1dda
created: 2026-09-07T08:00:41.752375Z
---
# Make commit force prefixes and cohort ownership explicit

Replace implicit mutable-tail force completion and implicit cohort retention with an explicit force target and matching retained-cohort identity in the existing commit path. Keep scheduling serial through this architecture checkpoint; tic-f1bb is the immediate consumer. Preserve synchronous durability and measure for regression, with no independent TPS gain requirement.

## Outcome

The existing synchronous commit path owns a frozen force target (WAL generation,
start/end offsets, final commit sequence and record count) and a matching
retained-cohort identity with its existing admitted lifetime. The next scheduling ticket can retain
that state without reinterpreting acknowledgement, pin release, or failure rules.
This is an architecture delivery with no independent TPS gain requirement.

## Owning mechanism and boundary

`tic-b368` defines the accepted contract and exact safe split before code begins.
The WAL owner defines force coverage; existing transaction/publication owners
retain acknowledgement, visibility, and cleanup. Replace superseded state and
all River-owned callers together. Keep the physical writer, queue, and serial
schedule in this checkpoint. `tic-f1bb` is the named immediate consumer.

The reconciliation must distinguish live synchronous ownership improvements
from mechanisms that can only be validated together with overlap. Any latter
part belongs atomically in `tic-f1bb`; do not install unused parallel machinery.
A new force-I/O execution owner needs an explicit reviewed architecture decision,
not an incidental second commit executor.

## Non-goals

No concurrent scheduling, batching delay, workload/client/protocol change,
isolation relaxation, weaker synchronous durability, WAL format change, or
independent throughput promise. No arbitrary cohort cap, second commit path,
compatibility mode, or speculative resource framework.

## Acceptance criteria

- The accepted b368 matrix maps each state, force prefix, lifetime, failure,
  cancellation, and recovery obligation to one existing owner and focused tests.
- The real synchronous path exercises every new contract; held-force and fault
  tests prove exact coverage, no premature acknowledgement/page write, fencing,
  and exactly-once resource release. Direct/group/quorum consumers remain covered.
- Hot-path allocation/copies remain explained; slopmark baseline and touched-file
  comparison do not introduce duplicate policy or unreviewed responsibilities.
- From the latest pushed stable checkpoint, capture two identical fresh short
  `tps-test` samples before production edits and two after, pinning the JDK and
  all workload/durability settings. Investigate repeated directional shifts with
  longer interleaved controls. Neutral TPS is expected; unexplained regression
  blocks acceptance. User-declared background load remains recorded.
- Independent concurrency/recovery review, affected-module tests, clean full test
  checkpoint, policy delta review, merge commit, annotated checkpoint tag and
  pushed delivery evidence complete the slice.

## Readiness

The accepted b368 design is the direct prerequisite. P0 (`tic-1dda`) is linked
as the broader promotion campaign and remains a hard gate on f1bb. Independent
concurrency/recovery review confirmed that its scheduler-causality instrumentation,
terminal snapshot export, stronger host observations and full scaling matrix do
not supply a missing invariant for this serial identity replacement. The initial
mapping carried historical P1 ordering into this narrowed code scope; this explicit
correction removes that false technical dependency without certifying P0.

The current path immediately exercises force coverage, retained identity and exact
release, using the tests/review/clean/TPS gates above. No concurrent scheduling,
multiple retained cohorts, changed locks or admission may enter this ticket. If
scope expands in those directions, this dependency disposition no longer applies.
Cumulative admission/chunking and the broader P0 gate remain on f1bb.

## Implementation checkpoint

The serial path captures `LocalWalForceTarget` before force I/O, retaining the
provider, WAL generation, non-wrapping token, exact range/count and final durable
CSN. Local force and configured durability success are distinct. Release and
cursor access require that target and its retained token; stale cursors cannot
read storage after release/reuse/rotation. An exhausted token space returns
`RESOURCE_EXHAUSTED` before force; the owner fences an already appended failed
commit and closes/reopens the WAL rather than wrapping identity.

Group/direct/vacuum consumers match captured append coverage before progressing
and retain the release token. Quorum reads and advances its watermark from the
captured target. The provider retains one target, with no scheduling or resource
admission change. Failure retains ownership until fenced close; exceptional
provider unwind releases the in-progress I/O exclusion and fences incomplete
work. The old result API, mutable-tail completion and global release/cursor APIs
are removed together.

Independent recovery review accepted the implementation after finding and fixing
exceptional-force unwind cleanup. Validation and promotion evidence follow below.

## Validation and acceptance — 2026-09-07

Implementation and measured candidate `91b00ea`, based on pushed `ab4769c`
(the production state of `perf-checkpoint-20260907-savepoint-admission`).
Independent recovery review and final evidence review accepted this serial
architecture delivery. No repeated regression was identified in the retained
diagnostics; performance equivalence and a speedup are not established.

Seven new WAL target tests cover held-I/O coverage/admission, local force failure,
reentrant fence/close, exceptional unwind, stale reuse/rotation, foreign providers
and token exhaustion. Quorum loss and continuation/final token tests are strengthened.
An engine fault test injects extra WAL coverage and proves the retained cohort is
not acknowledged. Existing held-force, recovery, read-dependency, direct/group,
maintenance, catalog and allocation suites remain in the affected/full gates.

- Affected modules: 33 WAL and 1,008 engine tests pass.
- Clean checkpoint: `GRADLE_USER_HOME=/private/tmp/river-gradle-tic-f8dd
  ./gradlew clean test --no-fail-fast verifyHotPathBytecodeFixtures` passes in
  7m42s: 1,796 tests, zero failures/errors, two existing benchmark skips.
- Indexed-table class references and bytecode policy fixtures pass. Fresh stable
  control and final candidate source/bytecode policies remain failing: 261 versus
  259 existing violations, zero added. Updating the two stale affected WAL method
  selectors removes those two failures; no allowance or threshold is weakened.
- Slopmark: LocalWal 145.405→147.523; group appender 70.7069→71.4171;
  vacuum writer 28.4502→31.1973; force coordinator 5→14.1504; direct committer
  and force target 0. The first vacuum draft reached 47.0012: review stopped
  behavior work and centralized exact append coverage in the target contract.
  The coordinator increase is the reviewed exceptional-unwind cleanup within
  its existing force responsibility. Other touched production scores are unchanged.
- Target/cursor/cohort carriers are reused; the WAL and quorum allocation gates
  pass with unchanged limits and no new per-force allocation or payload copy.

All runs pin `RIVER_JAVA=/opt/homebrew/Cellar/openjdk/26.0.2.1/libexec/openjdk.jdk/Contents/Home/bin/java`
(OpenJDK 26.0.2.1), tiny/standard, serializable, synchronous WAL, one warehouse,
ten terminals, seed 42, no-wait stress, 32 maximum attempts and unchanged budgets.
`./make.sh` builds each source before its workload; builds, tests and workloads
are serialized. The command is `tools/tps-test.sh --seed=42
--warmup-seconds=<warmup> --measured-seconds=<duration> --output-dir=<artifact>/<sample>`.

| Order | Source | Warmup / measured | TPS |
| --- | --- | --- | ---: |
| baseline-1, before production edits | ab4769c | 1s / 10s | 155.500 |
| baseline-2, before production edits | ab4769c | 1s / 10s | 157.200 |
| candidate-1 | 91b00ea | 1s / 10s | 167.400 |
| candidate-2 | 91b00ea | 1s / 10s | 163.800 |
| control-long-1 | ab4769c | 5s / 30s | 167.300 |
| candidate-long-1 | 91b00ea | 5s / 30s | 171.367 |
| control-long-2 | ab4769c | 5s / 30s | 180.833 |
| candidate-long-2 | 91b00ea | 5s / 30s | 166.033 |

The repeated upward short-sample shift triggered longer interleaving. The longer
pairs move +2.4% and −8.2%; neither direction repeats. Captured transactions per
force remain 1.007–1.011, local force means 3.336–3.410ms, and non-force writer
means 0.919–0.964ms per cohort. Force, preflight, append, publication, lock release
and main-family latency buckets show no repeated candidate worsening. The lower
TPS of the second candidate is retained, not discarded or attributed to host noise.

All eight samples have zero retries/errors, passing pre/post invariants, successful
performance/deadlock reconciliation and terminal receipts, clean stable sources,
and matching JDK/configuration fingerprints within each duration group. The user's
background PC load remains. Current TPS receipts do not independently establish
full-interval host exclusion or launched-class byte provenance; manual serialized
build/source evidence is retained. These are River diagnostics, not exclusive-host,
cross-database, P0-matrix or TPC-C capacity certification.

Immutable raw runs, XML, commands/logs, sample and mechanism summaries, independent
review, slopmark and policy delta are under
`/private/tmp/river-tic-7352-evidence-20260907`, with `SHA256SUMS`.
`tic-f1bb` retains its P0, cumulative admission/chunking and force-I/O contract gates.

## Delivery

Merged and pushed integration `195c6419afda3e288caf1f57f6e29aae5e51101c`, annotated checkpoint
`perf-checkpoint-20260907-force-target-ownership`. Feature branch
`ticket/tic-7352-force-target-ownership` is pushed. The exact integration's
1s/3s post-merge smoke passes with zero retries/errors and successful invariants,
capture/reconciliation, cleanup and terminal receipt; its TPS is not comparative
performance evidence. The implementation is closed; overlap remains gated by its
separate dependencies.
