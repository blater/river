package io.riverdb.engine.table;

/** Bounded latency stages reported by indexed-table commit telemetry. */
public enum IndexedCommitStage {
  QUEUE_RESIDENCE,
  LOGICAL_PREPARATION,
  GROUP_PREFLIGHT,
  PREFLIGHT_RECLAIM,
  PREFLIGHT_VERSION_RESERVATION,
  PREFLIGHT_COMPILE,
  PREFLIGHT_WAL_PLAN,
  PREFLIGHT_WAL_ADMISSION,
  PREFLIGHT_LOGICAL_ROW_ADMISSION,
  PREFLIGHT_RESOURCE_ADMISSION,
  PREFLIGHT_PAGE_FREEZE,
  PREFLIGHT_OPERATION_ADMISSION,
  GROUP_ADMISSION,
  GROUP_APPEND,
  GROUP_FORCE,
  GROUP_PUBLICATION,
  GROUP_PUBLICATION_PREPARE,
  GROUP_PUBLICATION_INSTALL,
  GROUP_TRANSACTION_COMPLETION,
  GROUP_LOCK_RELEASE,
  GROUP_LOCK_OUTCOME,
  GROUP_LOCK_REQUEST_CANCELLATION,
  GROUP_LOCK_HOLDING_RELEASE,
  GROUP_LOCK_RECORD_RECYCLE,
  GROUP_ACTIVE_REMOVAL,
  GROUP_OUTCOME_PUBLICATION,
  DIRECT_PREFLIGHT,
  DIRECT_APPEND,
  DIRECT_FORCE,
  DIRECT_PUBLICATION,
  DIRECT_RELEASE,
  WRITER_FAILURE,
  NOTIFICATION
}
